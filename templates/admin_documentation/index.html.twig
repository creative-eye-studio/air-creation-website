{% extends 'admin.html.twig' %}

{% block title %}Documentation{% endblock %}

{% block body %}
    <section class="block">
        <h2>Gestion de la documentation</h2>
        {{ form_start(docForm) }}
            <p>{{ form_label(docForm.product_name)}}<br/>
            {{ form_widget(docForm.product_name)}}</p>
            <p>{{ form_label(docForm.product_type)}}<br/>
            {{ form_widget(docForm.product_type)}}</p>
            <p>{{ form_label(docForm.submit)}}</p>
        {{ form_end(docForm) }}
        <hr />
        {% for cat in cats %}
            <div class="line">
                <p>
                    {{ cat.productname }} 
                </p>
                <ul>
                    <li><a href="{{ path('admin_doc_files', {id: cat.id}) }}">Ajouter des documents</a></li>
                    <li><a href="{{ path('admin_cat_manager', {id: cat.id}) }}">Modifier la catégorie</a></li>
                    <li><a href="{{ path('admin_doc_cat_delete', {id: cat.id}) }}">Supprimer</a></li>
                </ul>
            </div>
        {% endfor %}
    </section>
{% endblock %}